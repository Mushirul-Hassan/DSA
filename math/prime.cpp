#include <bits/stdc++.h>
using namespace std;
int isPrime(int n)
{
    while (n !=  0)
    {
      if ( n % )
    }
    return n;
}

int main()
{
    int n;
    cin >> n;
    if (isPrime(n))
       cout << "true";
    else
        cout << "false";
}
